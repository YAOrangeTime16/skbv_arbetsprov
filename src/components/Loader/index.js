import React from 'react';

const styles = {
  loader: {
    height: 200,
    padding: 20,
    fontSize: '1.5rem'
  }
}

const Loader = () => (<div style={styles.loader}>Loading information...</div>)

export default Loader;